Makes an `<Icon />` clickable and adds a larger hit box around the icon to make pressing it easier.

**Example**

    import {IconButton} from 'library'

    <IconButton icon="close" onClick={() => alert('Pressed!')} />
